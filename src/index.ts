// Package root entry: `@bemayker/mantsu-design-system`.
// Components and tokens. Icons are a separate entry point (`/icons`) so an
// app that only needs tokens does not pull in the icon set.
export * from './components/index';
